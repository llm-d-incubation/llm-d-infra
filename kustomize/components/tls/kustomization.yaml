apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# This component adds HTTPS listener with TLS termination
# Requires: TLS certificate Secret named 'llm-d-tls-cert'

patches:
  - path: gateway-tls-listener.yaml
    target:
      kind: Gateway
      name: llm-d-gateway